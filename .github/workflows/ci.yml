name: CI

on: [ push, pull_request ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - uses: php-actions/composer@v6
        with:
          args: validate
